# Installation V2 Implementation Baseline

Captured: 2026-09-18, before M0 production changes.

## Repository

- Branch: `work/final-no-xcode-setup-v1`
- HEAD: `fd4fbfe364383f83b053ae13e6c96ec3af4fbfd6`
- Product: Veya `0.1.0`, build `11`
- Architectures: `arm64`, `x86_64`
- Installed `/Applications/Veya.app`: universal main executable and provisioner; the architecture-reset snapshot records version `0.1.0 (11)`.
- All pre-existing dirty files and untracked qualification/research material were preserved. No reset, clean, stash, commit, or destructive state operation was performed.

## Toolchains

- Xcode `27.0 (27A266a)`
- Swift `6.4`, arm64 macOS host
- rustup `1.29.0`
- Active pre-M0 toolchain: floating `stable-aarch64-apple-darwin`, resolving to rustc `1.98.0`
- Cargo path: `/Users/rishiborra/.rustup/toolchains/stable-aarch64-apple-darwin/bin/cargo`; bare shell `cargo` is not available.
- Installed targets: `aarch64-apple-darwin`, `x86_64-apple-darwin`, `aarch64-apple-ios`.
- Installed components include Cargo, rustc, rustfmt, clippy, llvm-tools, rust-docs, and required standard libraries. This corrects the older planning observation that rustfmt was missing.

## Baseline results

- macOS Swift: 384 executed, 14 skipped, 0 failures.
- Rust bridge: 11 passed, 0 failed, 0 ignored using the rustup-resolved Cargo and `--locked`.
- iOS shared checks: `POCUnitChecks passed`.
- The first sandboxed iOS invocation failed before compilation because SwiftPM could not create its external module cache; rerunning with repository-local caches outside the command sandbox passed. This is an execution-environment issue, not a source failure.

## Skip classification

The 14 Swift skips are generated by opt-in guards across six categories:

- Real authorization/signing Keychain tests: `REQUIRED_RELEASE_GATE`; replace legacy codesign cases with M4 no-prompt wrapping-key tests.
- Native signing identity/codesign tests: `OBSOLETE_TEST` after M5/M11, but historical failure coverage must be replaced before deletion.
- Packaged app protocol test: `REQUIRED_RELEASE_GATE`.
- Live credentials-free Apple adapter probe: `LEGITIMATE_PLATFORM_SKIP` for normal unit runs; explicit qualification accounting required.
- Network DDI acquisition: `LEGITIMATE_PLATFORM_SKIP` in hermetic runs and `REQUIRED_RELEASE_GATE` for supported production builds.
- Current physical artifact/profile tests: `MISSING_INFRASTRUCTURE` until the production-driven harness supplies controlled fixtures.

No skipped test is accepted silently for Build 12.

## Current shipping paths to replace

- `ApplePersonalTeamLive.swift`, `VeyaSigningKeychain.swift`, `NativeSigningIdentityResolver.swift`, and `ConsumerArtifactProvisioner.swift` implement the private-key/SecIdentity/Keychain ACL/search-list/`/usr/bin/codesign` payload chain.
- `SetupStore`, `ConsumerOnboardingCoordinator`, `IOSSimSetupEngine`, helper commands, and current state stores overlap in setup ownership.
- Legacy state is written under `Application Support/IOSSim`, legacy Keychain services, and signing metadata paths.
- Build-time macOS application/DMG `codesign` calls in release scripts are distribution signing and are not payload-signer violations.

## Artifact preservation

Local Build 1-11 DMGs remain under `.build/iossim/local-release/`. No Build 12 artifact exists. Detailed hashes and physical state are preserved in `architecture-reset/00_PHASE_0_FREEZE_SNAPSHOT.md`.

